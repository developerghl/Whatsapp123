# ===========================================
# BACKEND ENVIRONMENT VARIABLES (.env)
# ===========================================

# Email Service Configuration (Choose ONE option)
# Option 1: Resend API (Recommended - Free tier available)
# RESEND_API_KEY=re_xxxxxxxxxxxxx
# EMAIL_FROM=Octendr <notifications@octendr.com>

# Option 2: SendGrid API
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxx
# EMAIL_FROM=notifications@octendr.com

# Option 3: Gmail SMTP (Requires App Password)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password-16-chars
# EMAIL_FROM=Octendr <your-email@gmail.com>
# EMAIL_REPLY_TO=your-email@gmail.com (Optional - defaults to SMTP_USER if not set)
#
# How to get Gmail App Password:
# 1. Go to Google Account → Security
# 2. Enable 2-Step Verification (if not already enabled)
# 3. Go to App Passwords → Generate new app password
# 4. Select "Mail" and "Other (Custom name)" → Enter "Octendr"
# 5. Copy the 16-character password and use it in SMTP_PASS
#
# Note: Do NOT use your regular Gmail password - it won't work!
# Gmail requires App Passwords for SMTP access.

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# LeadConnector / marketplace OAuth
GHL_CLIENT_ID=your_ghl_marketplace_client_id_here
GHL_CLIENT_SECRET=your_ghl_marketplace_client_secret_here
GHL_REDIRECT_URI=http://localhost:3000/oauth/callback
# Optional: defaults to https://marketplace.leadconnectorhq.com/oauth/chooselocation
# LC_MARKETPLACE_AUTHORIZE_URL=https://marketplace.leadconnectorhq.com/oauth/chooselocation

# Server Configuration
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:3001

# JWT Secret for Authentication (IMPORTANT: Keep this SECRET!)
JWT_SECRET=supersecretkey12345678901234567890

# ===========================================
# FRONTEND ENVIRONMENT VARIABLES (frontend/.env.local)
# ===========================================

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000
# Optional: must match backend LC_MARKETPLACE_AUTHORIZE_URL base (same default as backend)
# NEXT_PUBLIC_LC_MARKETPLACE_AUTHORIZE_URL=https://marketplace.leadconnectorhq.com/oauth/chooselocation

# Supabase Configuration (same as backend)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ===========================================
# PRODUCTION ENVIRONMENT VARIABLES
# ===========================================

# BACKEND (Render) - Set these in Render Dashboard
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
# GHL_CLIENT_ID=your_ghl_client_id
# GHL_CLIENT_SECRET=your_ghl_client_secret
# GHL_REDIRECT_URI=https://api.octendr.com/oauth/callback
# FRONTEND_URL=https://dashboard.octendr.com
# JWT_SECRET=supersecretkey12345678901234567890
# PORT=3000
# NODE_ENV=production

# FRONTEND (Vercel) - Set these in Vercel Dashboard
# NEXT_PUBLIC_API_URL=https://api.octendr.com
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# ===========================================
# GHL MARKETPLACE APP SETTINGS
# ===========================================
# Redirect URI: https://api.octendr.com/oauth/callback
# Scopes: locations.readonly users.readonly conversations.write
# Webhooks:
#   - Lead Webhook: https://api.octendr.com/webhooks/ghl
#   - Agent Reply Webhook: https://api.octendr.com/webhooks/ghl-agent